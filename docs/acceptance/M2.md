# M2 验收记录

- 日期：2026-08-12
- 范围：抖音解析与 `info` 任务
- 结论：已进入 M2，代码与测试定义完成；真实链接联调及 Go 工具链验证待执行

## 已完成

- [x] 从分享文案提取首个允许的 HTTPS 抖音链接，并生成作品 ID/canonical URL。
- [x] 限制协议、域名、端口、重定向次数，并在初始请求和每次跳转时校验 DNS/IP。
- [x] `_ROUTER_DATA` 与 `RENDER_DATA` 解析，统一返回视频/图文结构。
- [x] SQLite 作品缓存，默认 TTL 6 小时，解析器版本变化自动失效。
- [x] Fake Resolver 与输入、视频、图文、任务幂等测试定义。
- [x] `POST /api/v1/jobs` 与 `GET /api/v1/jobs/{id}`，M2 限定 `info` action。
- [x] 任务与作品按用户隔离，Idempotency-Key 持久化复用。

## 待验证

- [ ] 当前环境缺少 Go 工具链，尚未执行 `gofmt`、`go vet` 和 `go test`。
- [ ] 抖音页面结构会持续变化；需用脱敏、固定版本样本和真实公开视频验证解析器。
- [ ] 客户端 M5 UI 尚未实施，M2 出口条件中的“客户端提交并查看”暂由 API 契约覆盖，不能标记为完整验收。
