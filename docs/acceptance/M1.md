# M1 验收记录

- 日期：2026-08-12
- 范围：服务端核心与认证
- 结论：代码、迁移、测试定义与文档完成；运行测试待具备 Go 工具链的环境或 CI 验证

## 已完成

- [x] SQLite WAL、外键、busy timeout、事务化向前迁移与迁移幂等测试。
- [x] ULID 主键、用户、Refresh Token 会话与最小审计日志表。
- [x] Argon2id 密码哈希、HMAC-SHA256 Access Token、Refresh Token 哈希存储。
- [x] 登录、Refresh Token 轮换、退出、会话列表与指定会话撤销。
- [x] 账号创建、启用/禁用及密码重置管理命令；禁用和密码重置撤销全部会话。
- [x] Request ID、结构化 HTTP 日志、登录限速、存活与就绪检查。
- [x] 数据库、密码、Token、限速与 HTTP 基线测试代码。
- [x] API、配置和运行文档同步更新。
- [x] 经负责人确认，服务端 CI 暂时改为 GitHub Actions 手动触发，避免未配置 Go 环境时随 M1 推送自动执行。

## 未执行

- [ ] `gofmt`、`go vet`、单元/集成测试、Race 与 Linux 构建：当前环境没有 Go，且负责人要求不编译。
- [ ] 使用两个真实预创建账号完成登录、轮换、撤销演练：需要可运行环境和负责人提供最终账号名称/密码。

以上项目实际通过前，不把 M1 出口条件标记为已验证。

服务端工具链可用并完成首次验证后，应恢复 `push` 与 `pull_request` 自动触发。
