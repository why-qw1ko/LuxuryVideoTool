# M3 验收记录

- 日期：2026-08-12
- 范围：任务队列与媒体
- 结论：已进入 M3，代码与测试定义完成；按要求未编译、未依赖具体运行环境

## 已完成

- [x] SQLite 持久化队列、状态机、原子领取、默认单 Worker 与最大并发 2 限制。
- [x] Lease 60 秒、心跳 15 秒、过期 Lease 恢复与最大尝试次数控制。
- [x] `job_steps` 持久化解析/下载阶段与结构化失败信息。
- [x] `download` 任务异步创建、幂等、排队取消、处理中协作取消、失败重试。
- [x] 媒体流式下载、2 GB 默认上限、动态总超时、SHA-256/MIME/字节数登记。
- [x] 临时文件、0600 文件权限、0700 目录、安全相对路径与原子改名。
- [x] 文件归属校验、Range 下载和定时保留期清理。
- [x] 服务端 FFmpeg Adapter：参数数组、16 kHz/Mono/48 kbps MP3、超时、工作目录、截断日志、退出码检查。
- [x] 队列互斥领取、恢复、取消、路径逃逸与配置边界测试定义。

## 待环境验证

- [ ] 按要求未运行 `gofmt`、`go vet`、`go test` 或构建。
- [ ] 未进行真实抖音 CDN 下载、磁盘配额与 Linux 文件权限联调。
- [ ] 未进行真实 FFmpeg 进程、SIGTERM 与大文件压力验证。
- [ ] 两人并发提交与服务重启恢复已由代码和测试定义覆盖，仍需部署环境验收后才能标记出口条件通过。
