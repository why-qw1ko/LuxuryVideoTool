name: Client Disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-24.04
    steps:
      - name: Disabled
        run: echo "Android and Windows client builds are disabled."
