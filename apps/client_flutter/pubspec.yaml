name: douyin_capture
description: Douyin Capture client for Android and Windows.
publish_to: none
version: 0.1.0-dev.1

environment:
  sdk: ">=3.10.0 <4.0.0"
  flutter: ">=3.44.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
